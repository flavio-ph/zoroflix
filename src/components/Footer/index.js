import styles from "./Footer.module.css"

function Footer() {
    return (
        <footer className={styles.footer}>
            <h2>ZoroFlix | Desenvolvido por Roronoa Zoro</h2>
        </footer>
    );
}

export default Footer;